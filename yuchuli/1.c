#include <stdio.h>

#define str "hello"\
			"world"
#define SWITCH(s) whitch(char *s)
#define CASE CASE(char *)

#define max(a, b) ((a) > (b) ? (a):(b))
#define p(x) printf(#x " = %d\n" "2*" #x " = %d\n", x, 2 * x)

int main(void)
{

	int c = 100;;

	p(c);

	printf("%d\n", max(1, 2));
	


	return 0;
	printf("hello world\n");
	printf("%s\n", str);
	


	return 0;
}
